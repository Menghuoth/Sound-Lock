import React from "react";
import {Typography} from "antd";


const {Title} = Typography;

const AlbumInfo = props => {
    return (
        <Title style={{marginBottom: '40px'}}>Album Name</Title>
    );
};

export default AlbumInfo;